import React from 'react'
import { CustomColors } from './CustomColors';

export const CustomConfigs = {
  themeColors: {
    primary: {
      backgroundColor: CustomColors.themeColor.default,
    },
    secondary: {
      backgroundColor: CustomColors.themeColor.veyrDark,
    },
    muted: {
      backgroundColor: CustomColors.themeColor.dark,
    },
    light: {
      backgroundColor: CustomColors.themeColor.light,
    },
    veryLight: {
      backgroundColor: CustomColors.themeColor.veryLight,
    }
  }
}

